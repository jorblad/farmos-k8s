apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: farmos-sites-pvc
  namespace: farmos
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
  storageClassName: longhorn
